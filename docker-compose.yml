services:
  zenithbot:
    build: .
    container_name: zenithbot
    restart: unless-stopped
    env_file:
      - .env
    # Attach to the same network as Ollama
    networks:
      - zenith-net

# Tell Docker this network already exists (created in Step 1)
networks:
  zenith-net:
    external: true
