services:
  zenithbot:
    build: .
    container_name: zenithbot
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "20000:20000/tcp"
    volumes:
      - ./uploads:/zenith/uploads
    networks:
      - zenith-net

networks:
  zenith-net:
    external: true
